# Proposal: Re-tier the design leg — dispatcher authors + scaffold-reviews the design pre-dispatch; the implementor gets a locked design (#40)

> The canonical design doc (ADR + PR description). Reviewed by `--scaffold` before build. Lands on main.

## Problem

Ship-change's dispatcher contract splits WHO does the work by tier — a premium dispatcher session shapes and supervises, a dispatched execution-tier implementor runs the lifecycle — but it draws the leg boundary in the wrong place. Today the implementor authors the design doc (lifecycle steps 1–3) and then defends it under the cross-family `--scaffold` review. That parks design-defense judgment on the least-judgment-capable tier, whose characteristic failure mode is agreeable capitulation: silently implementing a plausible-but-wrong reviewer redesign rather than refuting it. Nothing in the lifecycle catches that failure — the review gate reads a capitulation as convergence.

The two incidents that bracket the gap, both on automated-researcher (see antondelafuente/automated-researcher#361 and its 2026-07-10 resolution comment):

- **antondelafuente/automated-researcher#347**: a premium dispatcher session ran the whole lifecycle itself — the mechanical legs (implement, review churn, merge ceremony) on the expensive tier, which the contract already forbids; the gates protect quality regardless of author.
- **antondelafuente/automated-researcher#364** (the dispatch that followed): an execution-tier implementor was handed a design-heavy ticket, and the dispatcher had to bolt on an ad-hoc mid-flight amendment ("findings contradicting the shaped design: refute with citation or escalate — never silently comply") because the lifecycle gave the implementor design-defense duties it isn't equipped for.

The sibling lifecycle got this right: experiment-lifecycle has the smart tier design WITH the researcher, cross-family-audits the design itself, and only then hands a locked brief to an executor with zero design freedom. Ship-change should have the same shape.

## Approach

**Move the design leg (steps 1–3) up to the dispatcher tier, uniformly for all tickets.** The researcher explicitly rejected a per-ticket "is this design-heavy?" triage bit — uniformity is the point. The split binds by ROLE (defined below), not universally — solo end-to-end use of the skill remains valid. Trivial tickets simply have trivial design docs, so the premium-tier cost of the design leg is negligible; the bulk of every ticket (implement + review churn) stays on the execution tier.

The product defines three deployment-neutral ROLES (scaffold finding 4); which concrete model/session maps to each role is instance configuration, not product text:

- **dispatcher** — a session at a tier the deployment reserves for judgment work (researcher-facing shaping, design, supervision).
- **dispatched implementor** — an execution-tier session launched by a dispatcher with a rendered brief.
- **solo** — a session running the lifecycle end-to-end as both designer and implementor, valid exactly as today. The split binds only when a dispatcher-role session is driving; which sessions those are is recorded in deployment guidance, not here (this deployment's mapping and the researcher's solo-role call are recorded on antondelafuente/automated-researcher#361).

**One handed-off lifecycle keeps ONE author family end-to-end (scaffold r2 finding 2):** the dispatcher and its dispatched implementor share the author family, and `<author>` stays constant across every verb of the run — preserving the cross-family invariant (the opposite family reviews BOTH the design and the code, and the native merge approval never comes from the author family). The rendered brief carries the author family; an implementor never re-chooses it.

The re-tiered leg split:

- **Dispatcher legs (judgment):** shape the issue with the researcher → `wf.sh start` → author the design doc → `wf.sh open` (draft PR) → `wf.sh design-review` → triage/defend the `--scaffold` findings as a peer. Design is judgment work, so authoring it at the dispatcher tier does not violate the contract — implementing does. After design-review triage converges, the design is **locked**.
- **Handoff (fail-closed, mechanical — scaffold finding 2):** "locked" is a verified state, not an assertion. A new `wf.sh dispatch <WORKTREE> <author>` verb gates the handoff, REUSING the trusted machinery that already exists rather than inventing a prose gate (scaffold r2 finding 1): `design-review` already stamps its posted review with the machine-readable trusted marker `pk=scaffold sha=<HEAD>` (wf.sh:1684), hardened per r4 F1 with a stamp-integrity guard (sha captured pre-review; no stamp if HEAD moved or the worktree dirtied while the review ran). `dispatch` verifies (a) the latest trusted scaffold marker's sha equals the branch's current HEAD, and (b) that same review reports ZERO HIGH findings (scaffold r3 finding 1: dispositions are structural-only — `disposition_gate.sh` checks structure, semantic soundness is the model reviewer's job — so a lock may never rest on an author-saved disposition; every fixed or refuted HIGH is re-reviewed until the reviewer itself reports none). On any mismatch — doc edited after review, marker missing/forged (non-trusted), HIGH present — it BLOCKS with the reason (fix/refute in the doc, re-run `design-review` to re-lock). Lock semantics in one line: **locked = the latest trusted cross-family scaffold review, at exactly the current HEAD, found zero HIGH.** On a clean gate it renders `references/DISPATCH-SPEC.md` with the parameters filled (repo, issue, worktree, branch, doc path, **and the locked sha**) and prints the ready-to-paste brief, making dispatch one verified command. Because the brief is consumed asynchronously, the lock is re-checked at BOTH ends (scaffold r3 finding 2, mirroring cloud-ship's `Reviewed-Head` rule): the rendered brief's step 0 makes the implementor verify the worktree is clean, on the named branch, and at exactly the locked sha — else STOP and return to the dispatcher. A stale brief can never start implementation on a moved or unreviewed design. Steps 4–6 (`implement`, `code-review <WORKTREE>`, `finish <WORKTREE>`) already take the worktree as an argument, so the implementor attaches to the dispatcher-created worktree rather than running `start`/`open`/`design-review` itself.
- **Implementor legs (execution):** implement in the worktree → `wf.sh code-review` + triage (findings concern its own code — the tier-appropriate fight) → `wf.sh finish` → print its DONE line and stop for reaping.

The concrete deliverables:

1. **SKILL.md — "Who runs this skill" rewrite.** The dispatcher-contract section carries the leg split above (duties 1–3 — model tier, watch, lifecycle — are unchanged and stay). The lifecycle code block gets a marked handoff boundary between steps 3 and 4.
2. **SKILL.md — the step-0 PRE-FLIGHT becomes the forcing function** antondelafuente/automated-researcher#361 asked for, with the sharper boundary this re-tier enables: "Dispatcher-tier session? Your legs END at design-review triage. STOP before step 4 — dispatch an execution-tier implementor with the locked design (see `references/DISPATCH-SPEC.md`). Implementor session? You start at step 4; if there is no reviewed design doc on the branch, STOP and return to your dispatcher — you never author or re-open the design."
3. **`references/DISPATCH-SPEC.md` — the dispatch-spec template**, so dispatching is one paste, cheaper than doing the work inline (the forcing function's economic half). Parameterized by repo, issue, worktree, branch, and locked design doc path. It bakes in, as standing policy rather than ad-hoc amendment: (a) scope = steps 4–6 only; (b) the escalation rule, with NO classification judgment on the implementor (scaffold finding 3): any code-review or merge-gate finding that conflicts with — or would alter — a decision recorded in the locked design **escalates to the dispatcher by default**, full stop. Escalation is an EXCLUSIVE HANDBACK, single-writer at all times (scaffold r2 finding 3): the implementor commits + pushes its work-in-progress, reports the escalation, and QUIESCES — no further worktree writes. The dispatcher then owns the worktree: amend → re-`design-review` → re-lock (re-run `wf.sh dispatch`) for escalations it accepts, or an answer-with-citation for ones it rejects; either way the implementor resumes only on the dispatcher's re-dispatch/answer, so a review marker can never bind to a moving or unpushed HEAD. Findings on the implementor's own choices (naming, layout, code quality) are handled normally; (c) the termination protocol (r4 F3): `IMPLEMENTOR DONE: PR <n> merged at <sha>` is the SIGNAL for the dispatcher's watch (duty 2) — it does not replace self-termination. The template ends with a deployment-supplied termination placeholder (filled by the launcher/instance, per contract duty 3), keeping the existing "the dispatch spec ends with a self-termination instruction" requirement intact; the dispatcher's reap remains the backstop.

4. **`wf.sh` — the `dispatch` verb + a stamp-integrity guard in the review path** (scaffold r1 F2, narrowed by r2 F1, corrected by r4 F1/F2): `dispatch` verifies trusted-marker-sha == HEAD and that the marked review itself reports zero HIGH (dispositions play no role in locking — r3 F1), then renders the template. And because the existing stamp reads HEAD only AFTER the long-running review returns (wf.sh:1671-1684), the review path gains a guard: capture the sha before the review starts, and refuse to stamp if HEAD or worktree cleanliness changed while it ran — a stamp must attest the content the reviewer actually read. Steps 4–6 untouched.

5. **Dispatch-gate behavior smoke, wired into `.aar-ci/checks.sh`** (scaffold r2 finding 6): covers the pass case and the block cases — missing review, non-trusted (forged) marker, stale sha (doc edited after review), review with HIGH present, GitHub lookup failure, dirty worktree, literal-safe template rendering, and the ambient-identity directed failure (below).

6. **Prerequisite declared: a trustable reviewer identity — gated on actual marker provenance, not configuration flags** (r3 F3, corrected by r4 F4: permissive-mode installs with a configured reviewer-bot token still produce a trusted marker and still work). `dispatch` fails DIRECTED exactly when it cannot recover a trusted marker from the expected reviewer login on the PR — naming what is missing (the reviewer-anchored review), the likely causes (no reviewer-bot identity configured; review posted under an unattested ambient identity), and pointing at the solo flow. No unattested-review compatibility path: a lock built on an unattested review is the vulnerability this verb closes.

**What enforces this, honestly tiered.** The WHO-does-which-leg boundary cannot be verified by `wf.sh` — a script can't see which model tier is driving it. Enforcement is therefore layered, strongest first:

- **The implementor side is structurally enforced by the brief.** A dispatched implementor's entire instruction set IS the template: it's briefed to start at step 4 against an existing worktree, and the template's first gate is "no reviewed design doc on the branch → STOP, return to dispatcher" — and `wf.sh dispatch` refuses to render a brief for an unreviewed/edited design in the first place, so a broken handoff fails at the dispatcher before a session is even launched. It is never handed an instruction that mentions authoring a design, so drifting into the design leg requires disobeying its brief, not merely skipping prose.
- **The dispatcher side is the pre-flight line + the dispatcher's own standing rules** (skill prose, instance memory, the researcher's catch — the same layers that failed in antondelafuente/automated-researcher#347, now with the boundary stated sharply enough to trip on). This is the weakest layer and the one that incident proved fallible; the template is what changes the economics, making dispatch one verified command (`wf.sh dispatch`) that is cheaper than implementing inline — removing the incentive that made the incident happen.
- **Optional mechanical hardening (deployment-owned, out of scope here):** the instance launch seam that already pins the implementor's model can also export a role marker (e.g. `WF_ROLE=implementor`) into the dispatched session's environment, and `wf.sh start`/`open`/`design-review` could refuse under it — making "implementor re-opens the design leg" mechanically impossible one-directionally. Deliberately not built now: the brief already covers it, and the reverse direction (dispatcher implementing) is unenforceable by env var anyway since the dispatcher's environment is the default one. Noted so the option is on the record if the procedural layers leak.

Evidence seam: antondelafuente/automated-researcher#364 is running as the last old-flow dispatch at the time of writing; its `--scaffold`/`--code` review rounds are live evidence for the exact SKILL.md wording (does an execution-tier author get clobbered on shaped decisions?) and will be cited in the section's rationale line the way cloud-ship's SKILL.md cites its own motivating incident.

## Alternatives considered

- **Dispatcher does everything (repeal the split).** Rejected: repeals the contract's economics for no quality gain — the cross-family review + fail-closed gates protect quality regardless of author tier (the rationale documented in cloud-ship's SKILL.md), and antondelafuente/automated-researcher#347 is the incident showing the cost. The dispatcher's session also stops being researcher-available while it grinds mechanics.
- **Per-ticket triage ("design-heavy → split; mechanical → old flow").** Rejected by the researcher explicitly: it reintroduces a per-ticket judgment call, and the marginal saving (skipping a trivial design doc) is negligible.
- **Escalation-seam-only (keep implementor-authored design; route design-touching findings to the dispatcher).** Rejected as the default: it relocates a classification judgment ("is this finding design or detail?") onto the execution tier, and misclassification fails silently as capitulation — the exact failure being fixed. The residual case (a post-lock finding that touches the design) is handled without relocating that judgment: it escalates to the dispatcher by default (see deliverable 3b) — the implementor matches findings against the locked doc's recorded decisions, a lookup, not a judgment call.

## Blast radius

- **This skill only:** SKILL.md prose (dispatcher-contract section + step-0 pre-flight + lifecycle block annotations) and a new `references/DISPATCH-SPEC.md`. Plugin version bump per repo convention.
- **`wf.sh`: one additive verb (`dispatch`); no existing verb changes** (the sha-stamped trusted marker and `fdispo` state it reads already exist). Steps 4–6 untouched, so solo/end-to-end flows are unaffected. Plus the dispatch-gate smoke in `.aar-ci/checks.sh`.
- **cloud-ship: out of scope for the mechanism, tracked as #42 — but the routing guidance changes NOW** (scaffold r1 finding 1, sharpened by r2 finding 4, which corrected this doc's earlier premise: cloud-ship's author leg explicitly DEFAULTS to execution-tier — `claude-sonnet-5`, per its SKILL.md — so the motivating failure mode is live there, not hypothetical). Extending the locked-design handoff to the two-machine flow still needs its own shaping (#42); what rides along here is the honest interim routing change — covering EVERY place ship-change directs readers to cloud-ship, including the skill frontmatter and the top routing paragraph, not just a buried pointer line (r4 F5): dispatcher-role sessions prefer the on-box re-tiered flow; routing a change to cloud-ship's implementor-authored design leg is a researcher-approved exception until #42 lands.
- **Instance side (not this repo):** how a dispatcher launches/pins/watches implementor sessions stays deployment-owned (the contract already says so). The template references the seams generically.

## Rollout + rollback

Docs + template + one additive `wf.sh` verb; one-commit revert restores the old flow exactly (nothing else calls `dispatch`). Self-referentially staged: this very ticket runs the new flow — this design doc is dispatcher-authored and will be scaffold-reviewed pre-dispatch, and the implement leg (#40's steps 4–6) will be the first dispatch briefed by the new template's content. If the handoff mechanics fail in practice, the failure is visible in the first dispatch and the fallback (implementor runs the old full lifecycle) remains available until this merges.
